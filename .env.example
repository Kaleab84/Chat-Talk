PINECONE_API_KEY=
PINECONE_INDEX_NAME=cfc-animal-feed-chatbot-test
PINECONE_CLOUD=aws
PINECONE_REGION=us-east-1
PINECONE_INDEX_NAME_VIDEOS = cfc-videos
PINECONE_NAMESPACE=cfc-videos
EMBED_MODEL=all-MiniLM-L6-v2
# Optional: Supabase Storage (if used)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_BUCKET_VIDEOS= cfc-videos

# Optional: AI API (enables AI answers in chat)
# NOTE: Secrets cleared for shared use; set locally when running the app.
OPENAI_API_KEY=
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
SESSION_SECRET=


SESSION_COOKIE_NAME=CFC_SESSION
SESSION_TTL_DAYS=7
FRONTEND_BASE_URL=http://localhost:3000
BACKEND_BASE_URL=http://localhost:8000
# if CORS_ORIGINS is used:
CORS_ORIGINS=http://localhost:3000,http://localhost:8000
ENV=development

# For production (when CFC website is live)
CFC_WEBSITE_URL=https://cfc-website.com

# For development/testing (disable protection)
ENABLE_REFERRER_PROTECTION=false